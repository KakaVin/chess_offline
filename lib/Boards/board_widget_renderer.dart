import 'package:chess_offline/Pieces/piece.dart';
import 'package:flutter/material.dart';

import 'board.dart';

class BoardWidgetRenderer{
  Widget render(Board board, Piece? piece){

    return const Center(
      child: Text("start coding interface"),
    );
  }
}